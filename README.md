# sa.ReactNative2semestre

informações :{

    nome completo = input(nome completo),

    idade = input(idade),

    alergias= input(alergias) --input pode conter um texto grande com varias alergias 

    contatos = input(
                    seu telefone,
                    nome do contato de emergencia,
                    telefone contato de emergencia
                    ),

    endereço = input( 
                    CEP,
                    Rua,
                    numero
                    )
    

}